\* Byzantine weight ONE OVER the 1/3 threshold (3 of 7).
\* Expected result: Safety is VIOLATED — TLC produces a counterexample where
\* three equivocating validators make both conflicting cuts finalize. This
\* demonstrates the 1/3 fault bound is tight.
SPECIFICATION Spec
CONSTANTS
    Validators = {v1, v2, v3, v4, v5, v6, v7}
    MaxByzNum = 3
INVARIANT Safety
